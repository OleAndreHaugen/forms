const deleteItem = oEvent.getParameter("listItem");
const context = deleteItem.getBindingContext();
const data = context.getObject();

let elementIndex = context.sPath.split("/")[2];
ModelData.Delete(modeloPageDetail.oData.setup[elementIndex].items, "id", data.id);
modeloPageDetail.refresh();

// deleteItem?.getParent()?.fireEvent("change");