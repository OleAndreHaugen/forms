let selected = oEvent.getParameter("selectedKey");

switch (selected) {
    case "preview":
        controller.preview();
        break;

    case "designer":
        if (controller.currentParent) {
            modeloPageElements.setData(controller.currentParent);
            modeloPageElements.refresh();
        }
        break;

    default:
        break;
}
